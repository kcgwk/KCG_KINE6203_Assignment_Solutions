function output = isNumbPrim(num)
% input is num (whole number)
% output is logical true/false if num is prime, uses matlab's built in
% isprime
output = isprime(num); % returns 1 (truuu) for prime, 0 (false) if not
end