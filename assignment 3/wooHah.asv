function result = wooHah(a, b) % ok inputs once again (a,b) ur numbs, outputs are woo if the combo is even and hah if its not
if mod(a + b, 2) == 0 % this checks if the sum is even
    result = 'Woo'; % we are go for even
else
    result = 'Hah'; % we are go for odd 
end
end